# Data files for manuscript figures

This directory contains the CSV files used by the manuscript data figures.
The files are copied from `experiment/data/` and match the data list in
`experiment/README.md`.

The notebooks in `experiment/notebooks/` are the main execution entry points.
Running a notebook writes its CSV files to `experiment/data/` and saves the
corresponding figure files to `experiment/figures/`.
